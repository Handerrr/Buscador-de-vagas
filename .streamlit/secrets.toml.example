# Copie somente os valores necessários para a área Secrets do Streamlit Cloud.
# Nunca renomeie este exemplo para secrets.toml dentro do Git.

DATABASE_URL = "postgresql://usuario:senha@servidor/banco?sslmode=require"
DASHBOARD_DEMO_MODE = "true"
